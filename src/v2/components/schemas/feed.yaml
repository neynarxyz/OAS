ForYouProvider:
      type: string
      default: openrank
      enum:
        - openrank
        - mbd
      description: |
        The provider of the For You feed.

FeedTrendingProvider:
      type: string
      enum:
        - neynar
        - openrank
        - mbd
      default: neynar
      description: |
        The provider of the trending casts feed.

